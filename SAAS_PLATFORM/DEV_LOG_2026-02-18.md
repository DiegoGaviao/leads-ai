# Log de Desenvolvimento - Leads AI SaaS
Data: 18/02/2026 (Fim do dia)

## Status Atual
O sistema evoluiu de um protótipo visual para um SaaS funcional com persistência de dados e processamento de IA em background.

## Realizações do Dia
1.  **Frontend & Design**:
    - Landing Page refeita com design system "High-Ticket" (estilo Linear).
    - Planos reestruturados: Free (Trial), Pro (Strategy), Master (Data Audit).
    - Onboarding transformado em "Workshop de DNA": coleta Tom de Voz, Autoridade, Dores, Sonhos em profundidade.

2.  **Persistence & Database**:
    - Implementação da conexão real com Supabase (`src/lib/supabaseClient.ts`, `src/services/LeadsRepository.ts`).
    - O formulário agora salva os dados nas tabelas `clients` e `briefings` ANTES da etapa de conexão social.

3.  **Backend & AI Worker**:
    - Criação do `backend/worker.py`: Processo em background que monitora novos clientes no banco.
    - Atualização do `backend/services.py`: Prompt da IA enriquecido com os novos dados do DNA da marca.
    - Fallback implementado: Tenta DeepSeek, se falhar, usa GPT-4o-mini.

4.  **Correções de Fluxo**:
    - Página de Conexão com Instagram agora possui URL de redirecionamento dinâmica (fix erro localhost em prod).
    - Adicionado botão "Pular Conexão" para evitar churn se a API do Facebook falhar.
    - Build de produção (`dist`) gerado e validado.

## Próximos Passos (Crítico)
1.  **Facebook App Mode**:
    - O Login com Facebook falha para usuários externos porque o App está em modo "Development".
    - **Ação:** Adicionar usuários como "Testers" no painel da Meta ou iniciar processo de "App Review" para Live Mode.
    
2.  **Deploy Backend**:
    - O `worker.py` roda localmente. Precisa ser deployado (Render/Railway) para processar leads 24/7.
    
3.  **Validação Final**:
    - Testar o fluxo completo em produção: Cadastro -> Banco -> Worker -> Dashboard com Roteiros.
